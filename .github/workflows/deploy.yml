name: Deploy GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build site
        env:
          NODE_ENV: production
          PUBLIC_BASE_PATH: /new-deliverance
          SITE_URL: https://autonateai.github.io/new-deliverance
        run: npm run build

      - name: Publish dist to gh-pages
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin gh-pages
          rm -rf .deploy
          git worktree add .deploy gh-pages
          find .deploy -mindepth 1 -maxdepth 1 ! -name '.git' -exec rm -rf {} +
          cp -a dist/. .deploy/
          touch .deploy/.nojekyll
          cd .deploy
          if [ -n "$(git status --short)" ]; then
            git add --all
            git commit -m "Deploy from ${GITHUB_SHA}"
            git push origin gh-pages
          else
            echo "No changes to publish."
          fi
          cd ..
          git worktree remove .deploy --force
